{"sections": [{"heading": "题目描述", "html": "<p><span>儿园两个班的小朋友在排队时混在了一起，每位小朋友都知道自己是否与前面一位小朋友同班，请你帮忙把同班的小朋友找出来。</span></p><p><span>小朋友的编号是整数，与前一位小朋友同班用Y表示，不同班用N表示。</span></p>", "type": "desc"}, {"heading": "输入描述", "html": "<p><span>输入为空格分开的小朋友编号和是否同班标志。</span></p><p><span>比如：6/N 2/Y 3/N 4/Y，表示4位小朋友，2和6同班，3和2不同班，4和3同班。</span></p><p><span>其中，小朋友总数不超过999，每个小朋友编号大于0，小于等于999。</span></p><p><span>不考虑输入格式错误问题。</span></p>", "type": "input"}, {"heading": "输出描述", "html": "<p><span>输出为两行，每一行记录一个班小朋友的编号，编号用空格分开，且：</span></p><ol start='' ><li><span>编号需按照大小升序排列，分班记录中第一个编号小的排在第一行。</span></li><li><span>若只有一个班的小朋友，第二行为空行。</span></li><li><span>若输入不符合要求，则直接输出字符串ERROR。</span></li></ol>", "type": "output"}, {"heading": "示例1", "html": "<p><span>输入</span></p><pre class=\"plain-code\">1/N 2/Y 3/N 4/Y</pre><p><span>输出</span></p><pre class=\"plain-code\">1 2\n3 4</pre><p><span>说明</span></p><blockquote><p><span>2的同班标记为Y，因此和1同班。</span><br/><span>3的同班标记为N，因此和1、2不同班。</span><br/><span>4的同班标记为Y，因此和3同班。</span><br/><span>所以1、2同班，3、4同班，输出为</span><br/><span>1 2</span><br/><span>3 4</span></p></blockquote>", "type": "sample"}, {"heading": "示例2", "html": "<p><span>输入</span></p><pre class=\"plain-code\">1/N 2/Y 3/N 4/Y 5/Y</pre><p><span>输出</span></p><pre class=\"plain-code\">1 2\n3 4 5</pre><p><span>说明</span></p><blockquote><p><span>无</span></p></blockquote>", "type": "sample"}, {"heading": "解题思路", "html": "<p><span>题目的要求是将一组小朋友按班级进行分类。输入由小朋友的编号和他们是否与前一位小朋友同班的标志组成。任务是根据这些标志将同班的小朋友归类并输出，遵循以下规则：</span></p><ol start='' ><li><p><span>输入是小朋友编号与他们是否与前一位小朋友同班的标志，用空格分隔。编号后面跟随一个标志：</span></p><ul><li><strong><span>Y</span></strong><span>：表示与前一个小朋友同班。</span></li><li><strong><span>N</span></strong><span>：表示与前一个小朋友不同班。</span></li></ul></li><li><p><span>根据这些标志，小朋友们要被分成</span><a href='https://so.csdn.net/so/search?q=%E4%B8%8D%E5%90%8C%E7%9A%84&amp;spm=1001.2101.3001.7020'><span>不同的</span></a><span>班级。</span></p></li></ol><h4><a name=\"示例分析\" class=\"md-header-anchor\"></a><span>示例分析</span></h4><h5><a name=\"示例-1\" class=\"md-header-anchor\"></a><span>示例 1</span></h5><p><span>输入：</span></p><pre class=\"plain-code\">1/N 2/Y 3/N 4/Y</pre><p><span>解释：</span></p><ul><li><span>小朋友1是第一个，所以他是班级1的第一个成员。</span></li><li><span>小朋友2与小朋友1同班（因为标志是Y），因此小朋友1和2同班，形成班级1。</span></li><li><span>小朋友3与小朋友2不同班（标志是N），因此小朋友3在另一个班级，形成班级2。</span></li><li><span>小朋友4与小朋友3同班（标志是Y），所以小朋友3和4同班，属于班级2。</span></li></ul><p><span>输出：</span></p><pre class=\"plain-code\">1 2\n3 4</pre><h5><a name=\"示例-2\" class=\"md-header-anchor\"></a><span>示例 2</span></h5><p><span>输入：</span></p><pre class=\"plain-code\">1/N 2/Y 3/N 4/Y 5/Y</pre><p><span>解释：</span></p><ul><li><span>小朋友1和2同班，属于班级1。</span></li><li><span>小朋友3和4同班，属于班级2。</span></li><li><span>小朋友5与4同班，属于班级2。</span></li></ul><p><span>输出：</span></p><pre class=\"plain-code\">1 2\n3 4 5</pre><p>&nbsp;</p><h1><a name=\"题解\" class=\"md-header-anchor\"></a><span>题解</span></h1><p><span>思路：</span></p><ul><li><span>将幼儿园小朋友假设为0班和1班的。将第一个小朋友放到0班。</span></li><li><span>接下来就按照后一个小朋友和前一个小朋友的关系决定他所处的班级。</span></li><li><span>输出前对两个班级小朋友的编号进行升序排序。先输出首个编号较小的班级编号，然后输出另一个班级的同学编号</span></li></ul>", "type": "solution"}, {"heading": "Go", "html": "<p>&nbsp;</p><p>&nbsp;</p><h4><a name=\"完整用例\" class=\"md-header-anchor\"></a><span>完整用例</span></h4><h5><a name=\"用例1\" class=\"md-header-anchor\"></a><span>用例1</span></h5><pre class=\"plain-code\">1/N 2/Y 3/N 4/Y</pre><h5><a name=\"用例2\" class=\"md-header-anchor\"></a><span>用例2</span></h5><pre class=\"plain-code\">1/N 2/Y 3/N 4/Y 5/Y</pre><h5><a name=\"用例3\" class=\"md-header-anchor\"></a><span>用例3</span></h5><pre class=\"plain-code\">6/N 7/Y</pre><h5><a name=\"用例4\" class=\"md-header-anchor\"></a><span>用例4</span></h5><pre class=\"plain-code\">1/N 2/N 3/N 4/Y 5/Y 6/Y 7/N 8/Y 9/Y 10/Y</pre><h5><a name=\"用例5\" class=\"md-header-anchor\"></a><span>用例5</span></h5><pre class=\"plain-code\">1/N 2/Y 3/N 4/Y 5/N 6/Y 7/N 8/Y 9/N 10/Y 11/N 12/Y 13/N 14/Y 15/N 16/Y 17/N 18/Y 19/N 20/Y</pre><h5><a name=\"用例6\" class=\"md-header-anchor\"></a><span>用例6</span></h5><pre class=\"plain-code\">1/N 2/N 3/N 4/N 5/N</pre><h5><a name=\"用例7\" class=\"md-header-anchor\"></a><span>用例7</span></h5><pre class=\"plain-code\">1/N 2/Y 3/N 4/Y 5/Y 100/Y 101/N 102/Y 103/N 104/Y 105/Y</pre><h5><a name=\"用例8\" class=\"md-header-anchor\"></a><span>用例8</span></h5><pre class=\"plain-code\">1/N</pre><h5><a name=\"用例9\" class=\"md-header-anchor\"></a><span>用例9</span></h5><pre class=\"plain-code\">6/N 7/Y 8/N 9/Y</pre><h5><a name=\"用例10\" class=\"md-header-anchor\"></a><span>用例10</span></h5><pre class=\"plain-code\">10/N 11/Y 12/N 13/Y 14/N</pre>", "type": "desc"}], "code_blocks": {"cpp": ["#include<iostream>\n#include<vector>\n#include<string>\n#include <utility> \n#include <sstream>\n#include<algorithm>\n#include<map>\n#include<vector>\nusing namespace std;\n​\n​\n​\nvector<string> split(const string& str, char delimiter) {\n    vector<string> result;\n    stringstream ss(str);\n    string item;\n    // 使用流读取，每次读取到分隔符\n    while (getline(ss, item, delimiter)) {\n        result.push_back(item);\n    }\n    return result;\n}\n​\n​\nint main() {\n    string tmp;\n    int last = -1;\n    // 记录位置幼儿和班级的映射, map会自动进行排序的\n    map<int, int> mp;\n    while (cin >> tmp) {\n        if (tmp == \"\"){\n            break;\n        }\n        vector<string> ans = split(tmp, '/');\n        int id = stoi(ans[0]);\n        // 输入数据合法性判断\n        if (id <= 0 || id > 999) {\n            cout << \"ERROR\";\n            return 0;\n        }\n        // 处理关系\n        if (last == -1) {\n            mp[id] = 0;\n        } else {\n            if (ans[1] == \"N\") {\n                mp[id] = !mp[last];\n            } else {\n                mp[id] = mp[last];\n            }\n        }\n        last = id;\n    }\n    \n    int classId  = -1;\n    for (int i = 0; i < 2; i++) {\n        bool flag =false;\n        for (const auto& pair : mp) {\n            int first = pair.first;\n            int second = pair.second;\n            if (!flag) {\n                flag = true;\n                if (classId == -1) {\n                    classId = second;\n                } else {\n                    classId = !classId;\n                }\n            }\n            if (second == classId) {\n                cout << first << \" \";\n            }\n        }\n        cout << endl;\n    }\n    return 0;\n}"], "java": ["import java.util.*;\n​\npublic class Main {\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        Map<Integer, Integer> mp = new TreeMap<>(); // 自动排序\n        int last = -1;\n​\n        while (scanner.hasNext()) {\n            String tmp = scanner.next();\n            String[] ans = tmp.split(\"/\");\n​\n            int id = Integer.parseInt(ans[0]);\n            // 输入数据合法性检查\n            if (id <= 0 || id > 999) {\n                System.out.println(\"ERROR\");\n                return;\n            }\n            // 处理关系\n            if (last == -1) {\n                mp.put(id, 0);\n            } else {\n                if (ans[1].equals(\"N\")) {\n                    mp.put(id, 1 - mp.get(last));\n                } else {\n                    mp.put(id, mp.get(last));\n                }\n            }\n            last = id;\n        }\n​\n        int classId = -1;\n        for (int i = 0; i < 2; i++) {\n            boolean flag = false;\n            for (Map.Entry<Integer, Integer> entry : mp.entrySet()) {\n                int first = entry.getKey();\n                int second = entry.getValue();\n                if (!flag) {\n                    flag = true;\n                    if (classId == -1) {\n                        classId = second;\n                    } else {\n                        classId = 1 - classId;\n                    }\n                }\n                if (second == classId) {\n                    System.out.print(first + \" \");\n                }\n            }\n            System.out.println();\n        }\n    }\n}\n​"], "python": ["import sys\n​\ndef main():\n    mp = {}  # 记录幼儿和班级的映射\n    last = -1\n​\n    for tmp in sys.stdin.read().split():\n        ans = tmp.split('/')\n        id = int(ans[0])\n​\n        # 输入数据合法性检查\n        if id <= 0 or id > 999:\n            print(\"ERROR\")\n            return\n        \n        # 处理关系\n        if last == -1:\n            mp[id] = 0\n        else:\n            mp[id] = 1 - mp[last] if ans[1] == 'N' else mp[last]\n        \n        last = id\n​\n    sorted_keys = sorted(mp.keys())  # 按 ID 排序\n    class_id = -1\n    for _ in range(2):\n        flag = False\n        for key in sorted_keys:\n            if not flag:\n                flag = True\n                if class_id == -1:\n                    class_id = mp[key]\n                else:\n                    class_id = 1 - class_id\n            if mp[key] == class_id:\n                print(key, end=\" \")\n        print()\n​\nif __name__ == \"__main__\":\n    main()\n​"], "js": ["const readline = require(\"readline\");\n​\nconst rl = readline.createInterface({\n    input: process.stdin,\n    output: process.stdout\n});\n​\nconst mp = new Map(); // 用 Map 记录关系\nlet last = -1;\n​\nrl.on(\"line\", (line) => {\n    let inputs = line.trim().split(/\\s+/); // 按空格分割输入\n    for (let tmp of inputs) {\n        let [id, relation] = tmp.split(\"/\");\n        id = parseInt(id);\n​\n        // 输入数据合法性检查\n        if (id <= 0 || id > 999) {\n            console.log(\"ERROR\");\n            process.exit();\n        }\n​\n        // 处理关系\n        if (last === -1) {\n            mp.set(id, 0);\n        } else {\n            if (relation === \"N\") {\n                mp.set(id, 1 - mp.get(last));\n            } else {\n                mp.set(id, mp.get(last));\n            }\n        }\n        last = id;\n    }\n​\n    let sortedKeys = Array.from(mp.keys()).sort((a, b) => a - b);\n    let classId = -1;\n    \n    for (let i = 0; i < 2; i++) {\n        let flag = false;\n        let result = [];\n        for (let key of sortedKeys) {\n            if (!flag) {\n                flag = true;\n                classId = classId === -1 ? mp.get(key) : 1 - classId;\n            }\n            if (mp.get(key) === classId) {\n                result.push(key);\n            }\n        }\n        console.log(result.join(\" \"));\n    }\n    process.exit();\n});\n​"], "go": ["package main\n​\nimport (\n    \"bufio\"\n    \"fmt\"\n    \"os\"\n    \"sort\"\n    \"strings\"\n    \"strconv\"\n)\n​\nfunc main() {\n    scanner := bufio.NewScanner(os.Stdin)\n    mp := make(map[int]int) // 记录幼儿与班级的映射\n    var ids []int           // 记录所有 id\n    last := -1\n​\n    // 读取输入\n    if scanner.Scan() {\n        inputs := strings.Fields(scanner.Text()) // 按空格分割\n        for _, tmp := range inputs {\n            parts := strings.Split(tmp, \"/\")\n            id, _ := strconv.Atoi(parts[0])\n​\n            // 输入数据合法性检查\n            if id <= 0 || id > 999 {\n                fmt.Println(\"ERROR\")\n                return\n            }\n​\n            // 处理关系\n            if last == -1 {\n                mp[id] = 0\n            } else {\n                if parts[1] == \"N\" {\n                    mp[id] = 1 - mp[last]\n                } else {\n                    mp[id] = mp[last]\n                }\n            }\n            ids = append(ids, id)\n            last = id\n        }\n    }\n​\n    // 按 ID 排序\n    sort.Ints(ids)\n    classId := -1\n​\n    // 输出结果\n    for i := 0; i < 2; i++ {\n        flag := false\n        var result []string\n        for _, id := range ids {\n            if !flag {\n                flag = true\n                if classId == -1 {\n                    classId = mp[id]\n                } else {\n                    classId = 1 - classId\n                }\n            }\n            if mp[id] == classId {\n                result = append(result, strconv.Itoa(id))\n            }\n        }\n        fmt.Println(strings.Join(result, \" \"))\n    }\n}\n​"]}}