{"sections": [{"heading": "题目描述", "html": "<p><span>A、B两个人玩抢7游戏，游戏规则为：</span></p><p><span>A先报一个起始数字 X（10 ≤ 起始数字 ≤ 10000），B报下一个数字 Y （X - Y &lt; 3），A再报一个数字 Z（Y - Z &lt; 3），以此类推，直到其中一个抢到7，抢到7即为胜者；</span></p><p><span>在B赢得比赛的情况下，一共有多少种组合？</span></p>", "type": "desc"}, {"heading": "输入描述", "html": "<p><span>起始数字 M。</span><code>1 ≤ M ≤ 10000</code></p>", "type": "input"}, {"heading": "输出描述", "html": "<p><span>B能赢得比赛的组合次数</span></p>", "type": "output"}, {"heading": "用例1", "html": "<h2><a name=\"输入\" class=\"md-header-anchor\"></a><span>输入</span></h2><pre class=\"plain-code\">10</pre><h2><a name=\"输出\" class=\"md-header-anchor\"></a><span>输出</span></h2><pre class=\"plain-code\">1</pre><p>&nbsp;</p><p>&nbsp;</p>", "type": "sample"}, {"heading": "题解", "html": "<p><span>思路：</span><code>动态规划</code></p><ol start='' ><li><p><span>其实这道题的思路一道经典题</span><code>爬楼梯方案数</code><span>非常相似。定义</span><code>dpA[]</code><span>其中</span><code>dpA[i]</code><span>表示A含数字i的方案数，定义</span><code>dpB[]</code><span>其中</span><code>dpB[j]</code><span>表示B喊B的方案数。</span></p></li><li><p><span>由题意可以</span><code>dpA[i]</code><span>的值由dpB[i+1]和dpB[i+2]的决定。</span><code>dpB[j]</code><span>类似。所以可以直接得出状态转移方程</span></p><ul><li><code>dpB[i] = dpA[i + 1] + dpA[i + 2]</code></li><li><code>dpA[i] = dpB[i + 1] + dpB[i + 2]</code></li></ul></li><li><p><span>明白2的状态方程之后，从m枚举到7就能直到，B能赢得比赛的组合数。对应结果为</span><code>dpB[7]</code><span>.</span></p></li></ol><p>&nbsp;</p><blockquote><p><span>额外注意，m的值[1,10000]，进行状态转移过程会出现非常大的数，经典的</span><code>大数相加</code><span>问题。对于没有内置大数的编程语言，需要手动定义函数实现(字符串相加)。</span></p></blockquote><p>&nbsp;</p><p>&nbsp;</p><h2><a name=\"c\" class=\"md-header-anchor\"></a><span>c++</span></h2><h2><a name=\"java\" class=\"md-header-anchor\"></a><span>JAVA</span></h2><h2><a name=\"python\" class=\"md-header-anchor\"></a><span>Python</span></h2><h2><a name=\"javascript\" class=\"md-header-anchor\"></a><span>JavaScript</span></h2><h2><a name=\"go\" class=\"md-header-anchor\"></a><span>Go</span></h2><p>&nbsp;</p>", "type": "solution"}], "code_blocks": {"c++": ["#include<iostream>\n#include<vector>\n#include<string>\n#include <utility> \n#include <sstream>\n#include<algorithm>\nusing namespace std;\n​\n​\n// 实现两个大整数的加法\nstring addBigNumbers(const string& num1, const string& num2) {\n    string res;\n    // 进位\n    int carry = 0;\n​\n    int i = num1.size() - 1;\n    int j = num2.size() - 1;\n​\n    // 从低位到高位逐位相加\n    while (i >= 0 || j >= 0 || carry) {\n        int digit1 = i >= 0 ? (num1[i--] - '0') : 0;\n        int digit2 = j >= 0 ? (num2[j--] - '0') : 0;\n        int sum = digit1 + digit2 + carry;\n        res += (sum % 10 + '0');\n        carry = sum / 10;\n    }\n​\n    reverse(res.begin(), res.end());\n    return res;\n}\n​\nint main() {\n    int m;\n    cin >> m;\n    // i 由 i+1 i+2 累加得来，所以定义长度为m + 2\n    vector<string> dpA(m + 2, \"0\");\n    dpA[m] = \"1\";\n    vector<string> dpB(m + 2, \"0\");\n    for (int i = m - 1; i >= 7; i--) {\n        // B得出的方案数 由 A叫 i + 1  + 叫 i+2 的方案数得来\n        dpB[i] =  addBigNumbers(dpA[i + 1],dpA[i + 2]);\n        // A得出的方案数 由 B叫 i + 1  + 叫 i+2 的方案数得来\n        dpA[i] = addBigNumbers(dpB[i + 1],dpB[i + 2]);\n    }\n    cout << dpB[7];\n    return 0;\n}"], "JAVA": ["import java.math.BigInteger;\nimport java.util.Scanner;\n​\npublic class Main {\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int m = scanner.nextInt();\n​\n        // i 由 i+1 i+2 累加得来，所以定义长度为 m + 2\n        BigInteger[] dpA = new BigInteger[m + 2];\n        BigInteger[] dpB = new BigInteger[m + 2];\n​\n        for (int i = 0; i < m + 2; i++) {\n            dpA[i] = BigInteger.ZERO;\n            dpB[i] = BigInteger.ZERO;\n        }\n​\n        dpA[m] = BigInteger.ONE;\n​\n        for (int i = m - 1; i >= 7; i--) {\n            // B得出的方案数 由 A叫 i + 1  + 叫 i+2 的方案数得来\n            dpB[i] = dpA[i + 1].add(dpA[i + 2]);\n            // A得出的方案数 由 B叫 i + 1  + 叫 i+2 的方案数得来\n            dpA[i] = dpB[i + 1].add(dpB[i + 2]);\n        }\n​\n        System.out.println(dpB[7]);\n    }\n}\n​"], "python": ["def main():\n    m = int(input())\n​\n    # i 由 i+1 i+2 累加得来，所以定义长度为 m + 2\n    dpA = [0] * (m + 2)\n    dpB = [0] * (m + 2)\n​\n    dpA[m] = 1\n​\n​\n    for i in range(m - 1, 6, -1):\n        # B得出的方案数 由 A叫 i + 1  + 叫 i+2 的方案数得来\n        dpB[i] = dpA[i + 1] + dpA[i + 2]\n        # A得出的方案数 由 B叫 i + 1  + 叫 i+2 的方案数得来\n        dpA[i] = dpB[i + 1] + dpB[i + 2]\n​\n    print(dpB[7])\n​\nif __name__ == \"__main__\":\n    main()\n​"], "js": ["const readline = require('readline');\n​\nconst rl = readline.createInterface({\n    input: process.stdin,\n    output: process.stdout\n});\n​\nlet inputLines = [];\n​\nrl.on('line', function (line) {\n    inputLines.push(line);\n}).on('close', function () {\n    let m = parseInt(inputLines[0]);\n​\n    // i 由 i+1 i+2 累加得来，所以定义长度为 m + 2\n    const dpA = Array(m + 2).fill(0n);\n    const dpB = Array(m + 2).fill(0n);\n​\n    dpA[m] = 1n;\n​\n    for (let i = m - 1; i >= 7; i--) {\n        // B得出的方案数 由 A叫 i + 1  + 叫 i+2 的方案数得来\n        dpB[i] = dpA[i + 1] + dpA[i + 2];\n        // A得出的方案数 由 B叫 i + 1  + 叫 i+2 的方案数得来\n        dpA[i] = dpB[i + 1] + dpB[i + 2];\n    }\n​\n    console.log(dpB[7].toString());\n});\n​"], "go": ["package main\n​\nimport (\n    \"fmt\"\n    \"math/big\"\n)\n​\nfunc main() {\n    var m int\n    fmt.Scan(&m)\n​\n    // i 由 i+1 i+2 累加得来，所以定义长度为 m + 2\n    dpA := make([]*big.Int, m+2)\n    dpB := make([]*big.Int, m+2)\n​\n    for i := 0; i < m+2; i++ {\n        dpA[i] = big.NewInt(0)\n        dpB[i] = big.NewInt(0)\n    }\n​\n    dpA[m] = big.NewInt(1)\n​\n    for i := m - 1; i >= 7; i-- {\n        // B得出的方案数 由 A叫 i + 1  + 叫 i+2 的方案数得来\n        dpB[i] = new(big.Int).Add(dpA[i+1], dpA[i+2])\n        // A得出的方案数 由 B叫 i + 1  + 叫 i+2 的方案数得来\n        dpA[i] = new(big.Int).Add(dpB[i+1], dpB[i+2])\n    }\n​\n    fmt.Println(dpB[7].String())\n}\n​"]}}