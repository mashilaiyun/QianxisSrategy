{"sections": [{"heading": "输入", "html": "<pre class=\"plain-code\">1\nApp1 1 09:00 10:00\n09:30</pre>", "type": "input"}, {"heading": "输出", "html": "<pre class=\"plain-code\">App1</pre>", "type": "output"}, {"heading": "说明", "html": "<blockquote><p><span>App1注册在9点到10点间，9点半可用的应用名是App1</span></p></blockquote><h1><a name=\"示例2\" class=\"md-header-anchor\"></a><span>示例2</span></h1>", "type": "note"}, {"heading": "输入", "html": "<pre class=\"plain-code\">2\nApp1 1 09:00 10:00\nApp2 2 09:10 09:30\n09:20</pre>", "type": "input"}, {"heading": "输出", "html": "<pre class=\"plain-code\">App2</pre>", "type": "output"}, {"heading": "说明", "html": "<blockquote><p><span>APP1和App2的时段有冲突，App2优先级高，注册App2之后，App1自动注销，因此输出App2。</span></p></blockquote><h1><a name=\"示例3\" class=\"md-header-anchor\"></a><span>示例3</span></h1>", "type": "note"}, {"heading": "输入", "html": "<pre class=\"plain-code\">2\nApp1 1 09:00 10:00\nApp2 2 09:10 09:30\n09:50</pre>", "type": "input"}, {"heading": "输出", "html": "<pre class=\"plain-code\">NA</pre><h1><a name=\"题解\" class=\"md-header-anchor\"></a><span>题解</span></h1><p><span>思路：</span><code>模拟</code></p><ul><li><span>将所有app限制的起始时间和终止时间转换为 int类型，方便进行比较。</span></li><li><span>创建</span><code>registeredApps</code><span>数组用于存储当前情况下能够有效注册的app。</span></li><li><span>遍历每个app注册请求，查询已认定有效注册app数组中是否存在</span><code>与当前app注册请求时间范围有冲突并且优先级比自己低的</code><span>， 有则在有效注册数组移除那个app注册记录，将自己加入有效注册数组中。</span></li><li><span>根据输入的时间，在有效注册app记录数组中寻找对应的APP。</span></li></ul>", "type": "output"}], "code_blocks": {"java": ["   import java.util.*;\n    \n    // 定义App类，用于存储App的相关信息\n    class App {\n        String name; // App名称\n        int priority; // App优先级\n        int startTime; // App允许使用的起始时间（以分钟为单位）\n        int endTime; // App允许使用的结束时间（以分钟为单位）\n    \n        // App类的构造函数，用于创建App对象\n        public App(String name, int priority, int startTime, int endTime) {\n            this.name = name;\n            this.priority = priority;\n            this.startTime = startTime;\n            this.endTime = endTime;\n        }\n    }\n    \n    public class Main {\n        // 时间转换函数，将时间字符串转换为以分钟为单位的整数\n        public static int convertTime(String time) {\n            String[] parts = time.split(\":\");\n            int hours = Integer.parseInt(parts[0]);\n            int minutes = Integer.parseInt(parts[1]);\n            return hours * 60 + minutes;\n        }\n    \n        public static void main(String[] args) {\n            Scanner scanner = new Scanner(System.in);\n            int n = scanner.nextInt(); // 读取App数量\n            List<App> apps = new ArrayList<>(); // 创建App列表，用于存储所有App\n    \n            for (int i = 0; i < n; i++) {\n                // 循环读取每个App的信息，并创建App对象添加到列表中\n                String appName = scanner.next();\n                int appPriority = scanner.nextInt();\n                String startTimeStr = scanner.next();\n                String endTimeStr = scanner.next();\n                int appStartTime = convertTime(startTimeStr);\n                int appEndTime = convertTime(endTimeStr);\n                apps.add(new App(appName, appPriority, appStartTime, appEndTime));\n            }\n    \n            String queryTimeStr = scanner.next();\n            int queryTime = convertTime(queryTimeStr); // 读取查询时间，并转换为分钟\n            String appAtTime = \"NA\"; // 初始化查询时间对应的App名称为\"NA\"\n    \n            List<App> registeredApps = new ArrayList<>(); // 创建已注册App列表\n            for (App app : apps) {\n                boolean canInsert = true;\n                List<Integer> needDeletePosition = new ArrayList<>();\n                if (app.startTime >= app.endTime) continue; // 如果起始时间不小于结束时间，则跳过\n    \n                // 遍历已注册的App列表，检查时间冲突\n                for (int i = registeredApps.size() - 1; i >= 0; i--) {\n                    App registered = registeredApps.get(i);\n                    if (Math.max(app.startTime, registered.startTime) < Math.min(app.endTime, registered.endTime)) {\n                        if (app.priority > registered.priority) {\n                            needDeletePosition.add(i); // 记录需要移除的位置\n                        } else {\n                            canInsert = false; // 当前App优先级不够高，无法插入\n                        }\n                    }\n                }\n    \n                if (canInsert) {\n                    registeredApps.add(app); // 插入当前App\n                    for (int i = 0; i <= needDeletePosition.size() - 1; i++) {\n                        registeredApps.remove((int) needDeletePosition.get(i)); // 移除冲突的App\n                    }\n                }\n            }\n    \n            // 遍历已注册App列表，找到查询时间对应的App\n            for (App app : registeredApps) {\n                if (queryTime >= app.startTime && queryTime < app.endTime) {\n                    appAtTime = app.name; // 更新查询时间对应的App名称\n                    break; // 找到后退出循环\n                }\n            }\n    \n            System.out.println(appAtTime); // 输出查询时间对应的App名称\n        }\n    }"], "python": ["   # 时间转换函数，将时间字符串转换为以分钟为单位的整数\n    def convert_time(time_str):\n        hours, minutes = map(int, time_str.split(':'))\n        return hours * 60 + minutes\n    \n    n = int(input())  # 读取App数量\n    apps = []  # 创建App列表，用于存储所有App\n    \n    for _ in range(n):\n        # 循环读取每个App的信息，并创建App对象添加到列表中\n        app_name, app_priority, start_time_str, end_time_str = input().split()\n        app_priority = int(app_priority)\n        start_time = convert_time(start_time_str)\n        end_time = convert_time(end_time_str)\n        apps.append((app_name, app_priority, start_time, end_time))\n    \n    query_time_str = input()\n    query_time = convert_time(query_time_str)  # 读取查询时间，并转换为分钟\n    app_at_time = \"NA\"  # 初始化查询时间对应的App名称为\"NA\"\n    \n    registered_apps = []  # 创建已注册App列表\n    for app in apps:\n        name, priority, start_time, end_time = app\n        if start_time >= end_time:\n            continue  # 如果起始时间不小于结束时间，则跳过\n        can_insert = True\n        need_delete_positions = []\n        # 遍历已注册的App列表，检查时间冲突\n        for i in range(len(registered_apps) - 1, -1, -1):\n            registered = registered_apps[i]\n            if max(start_time, registered[2]) < min(end_time, registered[3]):\n                if priority > registered[1]:\n                    need_delete_positions.append(i)  # 记录需要移除的位置\n                else:\n                    can_insert = False  # 当前App优先级不够高，无法插入\n        if can_insert:\n            registered_apps.append(app)  # 插入当前App\n            for pos in need_delete_positions:\n                registered_apps.pop(pos)  # 移除冲突的App\n    \n    # 遍历已注册App列表，找到查询时间对应的App\n    for app in registered_apps:\n        if query_time >= app[2] and query_time < app[3]:\n            app_at_time = app[0]  # 更新查询时间对应的App名称\n            break  # 找到后退出循环\n    \n    print(app_at_time)  # 输出查询时间对应的App名称"], "js": ["   // 时间转换函数，将时间字符串转换为以分钟为单位的整数\n    function convertTime(timeStr) {\n        const [hours, minutes] = timeStr.split(':').map(Number);\n        return hours * 60 + minutes;\n    }\n    \n    const readline = require('readline');\n    const rl = readline.createInterface({\n        input: process.stdin,\n        output: process.stdout\n    });\n    \n    let n;\n    const apps = [];\n    let queryTimeStr;\n    \n    rl.question('', (input) => {\n        n = parseInt(input); // 读取App数量\n        let count = 0;\n        rl.on('line', (line) => {\n            if (count < n) {\n                // 循环读取每个App的信息，并创建App对象添加到列表中\n                const [appName, appPriority, startTimeStr, endTimeStr] = line.split(' ');\n                const appStartTime = convertTime(startTimeStr);\n                const appEndTime = convertTime(endTimeStr);\n                apps.push({ name: appName, priority: parseInt(appPriority), startTime: appStartTime, endTime: appEndTime });\n                count++;\n            } else {\n                queryTimeStr = line; // 读取查询时间\n                rl.close();\n            }\n        });\n    });\n    \n    rl.on('close', () => {\n        const queryTime = convertTime(queryTimeStr); // 将查询时间转换为分钟\n        let appAtTime = \"NA\"; // 初始化查询时间对应的App名称为\"NA\"\n    \n        const registeredApps = []; // 创建已注册App列表\n        for (const app of apps) {\n            if (app.startTime >= app.endTime) continue; // 如果起始时间不小于结束时间，则跳过\n            let canInsert = true;\n            const needDeletePositions = [];\n            // 遍历已注册的App列表，检查时间冲突\n            for (let i = registeredApps.length - 1; i >= 0; i--) {\n                const registered = registeredApps[i];\n                if (Math.max(app.startTime, registered.startTime) < Math.min(app.endTime, registered.endTime)) {\n                    if (app.priority > registered.priority) {\n                        needDeletePositions.push(i); // 记录需要移除的位置\n                    } else {\n                        canInsert = false; // 当前App优先级不够高，无法插入\n                    }\n                }\n            }\n            if (canInsert) {\n                registeredApps.push(app); // 插入当前App\n                for (const pos of needDeletePositions) {\n                    registeredApps.splice(pos, 1); // 移除冲突的App\n                }\n            }\n        }\n    \n        // 遍历已注册App列表，找到查询时间对应的App\n        for (const app of registeredApps) {\n            if (queryTime >= app.startTime && queryTime < app.endTime) {\n                appAtTime = app.name; // 更新查询时间对应的App名称\n                break; // 找到后退出循环\n            }\n        }\n    \n        console.log(appAtTime); // 输出查询时间对应的App名称\n    });"], "c++": ["   #include <iostream>\n    #include <vector>\n    #include <string>\n    #include <sstream>\n    \n    // 定义App类，用于存储App的相关信息\n    class App {\n    public:\n        std::string name; // App名称\n        int priority; // App优先级\n        int startTime; // App允许使用的起始时间（以分钟为单位）\n        int endTime; // App允许使用的结束时间（以分钟为单位）\n    \n        // App类的构造函数，用于创建App对象\n        App(std::string name, int priority, int startTime, int endTime)\n            : name(name), priority(priority), startTime(startTime), endTime(endTime) {}\n    };\n    \n    // 时间转换函数，将时间字符串转换为以分钟为单位的整数\n    int convertTime(const std::string& time) {\n        int hours, minutes;\n        char colon;\n        std::istringstream iss(time);\n        iss >> hours >> colon >> minutes; // 将时间字符串按照\":\"分割并转换为小时和分钟\n        return hours * 60 + minutes; // 将小时和分钟转换为分钟\n    }\n    \n    int main() {\n        int n; // 读取App数量\n        std::cin >> n;\n    \n        std::vector<App> apps; // 创建App列表，用于存储所有App\n        for (int i = 0; i < n; i++) {\n            // 循环读取每个App的信息，并创建App对象添加到列表中\n            std::string appName;\n            int appPriority, appStartTime, appEndTime;\n            std::string startTimeStr, endTimeStr;\n            std::cin >> appName >> appPriority >> startTimeStr >> endTimeStr;\n            appStartTime = convertTime(startTimeStr);\n            appEndTime = convertTime(endTimeStr);\n            apps.emplace_back(appName, appPriority, appStartTime, appEndTime);\n        }\n    \n        std::string queryTimeStr;\n        std::cin >> queryTimeStr;\n        int queryTime = convertTime(queryTimeStr); // 读取查询时间，并转换为分钟\n        std::string appAtTime = \"NA\"; // 初始化查询时间对应的App名称为\"NA\"\n    \n        std::vector<App> registeredApps; // 创建已注册App列表\n        for (const App& app : apps) {\n            bool canInsert = true;\n            std::vector<int> needDeletePosition;\n            if (app.startTime >= app.endTime) continue; // 如果起始时间不小于结束时间，则跳过\n            // 遍历已注册的App列表，检查时间冲突\n            for (int i = registeredApps.size() - 1; i >= 0; --i) {\n                const App& registered = registeredApps[i];\n                // 如果存在时间冲突\n                if (std::max(app.startTime, registered.startTime) < std::min(app.endTime, registered.endTime)) {\n                    // 如果当前App的优先级高于已注册App的优先级\n                    if (app.priority > registered.priority) {\n                        // 记录起冲突需要移除的位置\n                        needDeletePosition.push_back(i);\n                    } else {\n                        // 之前注册app优先级比现在大或者相等，这个不能进行注册\n                        canInsert = false;\n                    }\n                }\n            }\n            // 未有冲突情况或者优先级是最高的那个情况下\n            if (canInsert) {\n                registeredApps.push_back(app);\n                //移除已经起冲突的注册\n                for (int i = 0; i < needDeletePosition.size(); i++) {\n                    registeredApps.erase(registeredApps.begin() + needDeletePosition[i]);\n                }\n            }\n            \n        }\n    \n        // 遍历已注册App列表，找到查询时间对应的App\n        for (const App& app : registeredApps) {\n            if (queryTime >= app.startTime && queryTime < app.endTime) {\n                appAtTime = app.name; // 更新查询时间对应的App名称\n                break; // 找到后退出循环\n            }\n        }\n    \n        std::cout << appAtTime << std::endl; // 输出查询时间对应的App名称\n    \n        return 0;\n    }"], "go": ["package main\n​\nimport (\n    \"bufio\"\n    \"fmt\"\n    \"os\"\n    \"strconv\"\n    \"strings\"\n)\n​\n// 定义App结构体，用于存储App的相关信息\ntype App struct {\n    name      string // App名称\n    priority  int    // App优先级\n    startTime int    // App允许使用的起始时间（以分钟为单位）\n    endTime   int    // App允许使用的结束时间（以分钟为单位）\n}\n​\n// 时间转换函数，将时间字符串转换为以分钟为单位的整数\nfunc convertTime(timeStr string) int {\n    parts := strings.Split(timeStr, \":\")\n    hours, _ := strconv.Atoi(parts[0])\n    minutes, _ := strconv.Atoi(parts[1])\n    return hours*60 + minutes\n}\n​\nfunc main() {\n    scanner := bufio.NewScanner(os.Stdin)\n    scanner.Scan()\n    n, _ := strconv.Atoi(scanner.Text()) // 读取App数量\n​\n    apps := []App{} // 创建App列表，用于存储所有App\n    for i := 0; i < n; i++ {\n        scanner.Scan()\n        line := scanner.Text()\n        parts := strings.Split(line, \" \")\n        appName := parts[0]\n        appPriority, _ := strconv.Atoi(parts[1])\n        startTimeStr := parts[2]\n        endTimeStr := parts[3]\n        appStartTime := convertTime(startTimeStr)\n        appEndTime := convertTime(endTimeStr)\n        apps = append(apps, App{name: appName, priority: appPriority, startTime: appStartTime, endTime: appEndTime})\n    }\n​\n    scanner.Scan()\n    queryTimeStr := scanner.Text()\n    queryTime := convertTime(queryTimeStr) // 读取查询时间，并转换为分钟\n    appAtTime := \"NA\" // 初始化查询时间对应的App名称为\"NA\"\n​\n    registeredApps := []App{} // 创建已注册App列表\n    for _, app := range apps {\n        if app.startTime >= app.endTime {\n            continue // 如果起始时间不小于结束时间，则跳过\n        }\n        canInsert := true\n        needDeletePositions := []int{}\n        // 遍历已注册的App列表，检查时间冲突\n        for i := len(registeredApps) - 1; i >= 0; i-- {\n            registered := registeredApps[i]\n            if max(app.startTime, registered.startTime) < min(app.endTime, registered.endTime) {\n                if app.priority > registered.priority {\n                    needDeletePositions = append(needDeletePositions, i) // 记录需要移除的位置\n                } else {\n                    canInsert = false // 当前App优先级不够高，无法插入\n                }\n            }\n        }\n        if canInsert {\n            registeredApps = append(registeredApps, app) // 插入当前App\n            for _, pos := range needDeletePositions {\n                registeredApps = append(registeredApps[:pos], registeredApps[pos+1:]...) // 移除冲突的App\n            }\n        }\n    }\n​\n    // 遍历已注册App列表，找到查询时间对应的App\n    for _, app := range registeredApps {\n        if queryTime >= app.startTime && queryTime < app.endTime {\n            appAtTime = app.name // 更新查询时间对应的App名称\n            break // 找到后退出循环\n        }\n    }\n​\n    fmt.Println(appAtTime) // 输出查询时间对应的App名称\n}\n​\n// 辅助函数：返回两个整数中的最大值\nfunc max(a, b int) int {\n    if a > b {\n        return a\n    }\n    return b\n}\n​\n// 辅助函数：返回两个整数中的最小值\nfunc min(a, b int) int {\n    if a < b {\n        return a\n    }\n    return b\n}\n​\n// 辅助函数：反转整数切片\nfunc reverse(slice []int) []int {\n    for i, j := 0, len(slice)-1; i < j; i, j = i+1, j-1 {\n        slice[i], slice[j] = slice[j], slice[i]\n    }\n    return slice\n}"]}}